import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'MuhammadNaufalSyarif_00000055788_IF670_AL_UTS',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
